package com.store
import scala.collection.{ mutable, immutable, generic }
import com.store.Inventory._
import com.store.Items._

class ShoeStore(storeInventory: Inventory = new Inventory, name: String = "Test Store") {
}
